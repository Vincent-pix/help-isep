# Copier vers frontend/.env.production avant le build

# Même serveur (recommandé sur VPS IONOS) : URL relative
VITE_API_URL=/api

# Vercel + Railway (recommandé) :
# VITE_API_URL=https://help-isep-production.up.railway.app/api
